# Notebooks

Use this folder for optional Jupyter notebooks.

The project is currently script-first:

- `src/eda.py` creates EDA charts
- `src/train.py` trains and evaluates models
- `src/predict.py` loads the saved model and predicts quality

